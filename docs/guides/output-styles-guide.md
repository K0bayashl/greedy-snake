# Claude Code Output Styles 配置指南

## 📌 什么是 Output Styles？

**Output Styles** 通过修改 Claude Code 的 **system prompt**，把 Claude Code 变成不同类型的 Agent。

```
不是简单的 "输出风格"，而是 "角色 + 行为 + 交互契约" 的完整重塑
```

### 核心概念

```
┌─────────────────────────────────────────────────────────┐
│              没有 Output Styles 的情况                    │
├─────────────────────────────────────────────────────────┤
│  Claude Code                                             │
│  │                                                       │
│  └─> 默认角色：偏执行、高效、落地的工程师                │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│              有 Output Styles 的情况                      │
├─────────────────────────────────────────────────────────┤
│  Claude Code                                             │
│  │                                                       │
│  ├─> Explanatory ──> 讲解型专家（插入教育性 Insights）   │
│  ├─> Learning ──> 共学型伙伴（插入 TODO 让你参与）       │
│  └─> Custom ──> 自定义角色（产品经理、运维专家等）      │
└─────────────────────────────────────────────────────────┘
```

## 🔧 配置文件位置

### 项目级配置
`.claude/output-styles/`（仅当前项目生效）

### 用户级配置
`~/.claude/output-styles/`（所有项目生效）

**优先级**：项目级配置会覆盖用户级配置

## 📝 基本使用方式

### 方式1：命令切换

```
/output-style explanatory
```

可用选项：
- `default` - 默认风格
- `explanatory` - 讲解型
- `learning` - 共学型

### 方式2：配置文件

```json
// .claude/settings.local.json
{
  "outputStyle": "explanatory"
}
```

## 🎯 三种内置风格

| 风格 | 特点 | 适用场景 |
|------|------|----------|
| **Default（默认）** | 偏执行、高效、落地 | 日常开发 |
| **Explanatory（讲解型）** | 插入教育性 Insights | 理解代码库、学习实现选择 |
| **Learning（共学型）** | 插入 `TODO(human)` 让你参与 | 结对编程、训练手感 |

### Default 风格

**特点**：
- 直接执行任务
- 高效简洁
- 专注于落地

**适用场景**：
- 日常开发工作
- 快速修复 Bug
- 常规功能实现

### Explanatory 风格

**特点**：
- 在回复中插入教育性 Insights
- 解释"为什么"这样做
- 帮助理解代码和设计决策

**适用场景**：
- 理解现有代码库
- 学习新的实现方式
- 代码审查和重构

**示例输出**：
```
我修改了 game.js 中的碰撞检测函数。

💡 Insight：原来的实现使用简单的坐标比较，新的实现
使用边界盒检测，这样可以更精确地处理碰撞。

修改原因：...
```

### Learning 风格

**特点**：
- 插入 `TODO(human)` 标记
- 让你参与决策和实现
- 结对编程体验

**适用场景**：
- 学习新技能
- 训练编程手感
- 教学演示

**示例输出**：
```
我建议添加一个新功能：道具系统。

TODO(human): 你想先实现哪种道具？
- 1. 加速道具
- 2. 减速道具
- 3. 无敌道具

请选择或提出你的想法。
```

## 🎨 自定义 Output Style

### 文件位置
```
项目级：.claude/output-styles/
用户级：~/.claude/output-styles/
```

### 文件格式

```markdown
---
name: Game Developer
description: 贪吃蛇游戏开发专家
keep-coding-instructions: true
---

# 游戏开发助手

你是贪吃蛇游戏的开发专家。

## 行为规则
- 修改代码前先 Read 文件
- 重要功能完成后更新文档
- 保持代码简洁，不添加依赖
```

### 关键字段说明

| 字段 | 说明 | 必填 |
|------|------|------|
| `name` | 显示名称 | ✅ |
| `description` | 在 `/output-style` UI 中展示的描述 | ✅ |
| `keep-coding-instructions` | 是否保留编码习惯 | ❌ |

### keep-coding-instructions 的作用

```
true  → 保留工程师习惯（运行测试、验证、谨慎执行）
false → 切换到非工程师角色（更偏文档/决策/模板）
```

**示例对比**：

```markdown
# keep-coding-instructions: true
修改后我会：
1. 运行测试验证
2. 检查是否有副作用
3. 更新相关文档
```

```markdown
# keep-coding-instructions: false
修改后我会：
1. 提供修改建议
2. 列出可能的影响
3. 等待你的确认
```

## 💡 实用配置示例

### 示例1：游戏开发专家

```markdown
---
name: Snake Game Dev
description: 贪吃蛇游戏开发专家
keep-coding-instructions: true
---

# 游戏开发助手

你是贪吃蛇游戏的开发专家。

## 项目背景
- 纯前端游戏，使用 Canvas API
- 已实现：基础游戏、难度选择、双倍分数食物
- 不使用框架和第三方库

## 代码风格
- 2 空格缩进
- 驼峰命名
- 函数名以动词开头

## 工作流程
1. 修改代码前先 Read 文件
2. 修改后说明变更内容
3. 重要功能完成后更新 learning.md

## 回复规范
- 用中文回复
- 不添加 emoji
- 简洁明确
```

### 示例2：产品经理

```markdown
---
name: Product Manager
description: 产品经理 - 专注需求和用户体验
keep-coding-instructions: false
---

# 产品经理助手

你是贪吃蛇游戏的产品经理。

## 关注重点
- 用户体验
- 功能价值
- 产品规划

## 工作方式
- 不直接修改代码
- 提供需求分析和建议
- 评估功能优先级

## 回复规范
- 从用户角度思考
- 提供多个方案对比
- 列出 pros 和 cons
```

### 示例3：运维专家

```markdown
---
name: DevOps Expert
description: 运维专家 - 专注部署和监控
keep-coding-instructions: true
---

# 运维专家助手

你是贪吃蛇游戏的运维专家。

## 关注重点
- 部署流程
- 性能监控
- 错误追踪

## 工作方式
- 优化部署脚本
- 添加监控日志
- 提供运维建议

## 回复规范
- 提供可执行的命令
- 说明潜在风险
- 给出回滚方案
```

## ⚙️ 配置详解

### 基本配置

| 配置项 | 说明 |
|--------|------|
| 文件位置 | `.claude/output-styles/{name}.md` 或 `~/.claude/output-styles/{name}.md` |
| 重启要求 | ⚠️ **必须重启** Claude Code |
| 生效时机 | 重启后生效 |
| 激活方式 | `/output-style {name}` 或配置 `settings.local.json` |

### 配置结构

**⚠️ 重点**：Output Style 文件必须有 YAML frontmatter

```markdown
---
name: Display Name
description: 简短描述
keep-coding-instructions: true
---

# 以下为自定义的 system prompt 内容
```

### YAML 字段说明

| 字段 | 必填 | 说明 | 可选值 |
|------|------|------|--------|
| `name` | ✅ | 显示名称 | 任意字符串 |
| `description` | ✅ | 简短描述 | 在 UI 列表中展示 |
| `keep-coding-instructions` | ❌ | 是否保留编码习惯 | `true`, `false`（默认 false） |

### keep-coding-instructions 详解

**⚠️ 重点**：这个字段决定了 Claude 是否保持"工程师身份"

| 值 | 行为特征 | 适用场景 |
|----|----------|----------|
| `true` | 保留编码习惯：运行测试、验证代码、谨慎执行 | 技术角色：架构师、开发者、测试 |
| `false` | 非工程师角色：偏文档、决策、模板 | 非技术角色：产品经理、运维、文档写作 |

### 目录结构

```
.claude/
├── output-styles/
│   ├── snake-game-dev.md
│   ├── product-manager.md
│   └── code-reviewer.md
```

### 激活方式

**方式1：命令切换**
```
/output-style snake-game-dev
```

**方式2：配置文件默认**
```json
// .claude/settings.local.json
{
  "outputStyle": "snake-game-dev"
}
```

### 常见问题

| 问题 | 解决方案 |
|------|----------|
| Output Style 不出现在列表中 | ⚠️ 重启 Claude Code |
| YAML frontmatter 格式错误 | 检查 `---` 是否成对，字段是否正确 |
| Claude 行为不符合预期 | 检查 `keep-coding-instructions` 设置 |

## 🔄 工作流程

```
1. 创建自定义 Output Style 文件
   │
2. 保存到 .claude/output-styles/ 目录
   │
3. 重启 Claude Code（加载配置）
   │
4. 使用命令切换风格
   │
5. Claude 按照新角色行为工作
```

## 🎨 Output Styles vs 其他概念

### 易混淆点对照

| 能力 | 作用位置 | 影响范围 | 用途 |
|------|----------|----------|------|
| **Output Styles** | System Prompt（主对话） | 整体角色、语气、输出契约 | 切换身份（产品/运维/写作） |
| **CLAUDE.md** | User Message | 项目级约束 | 项目规范、目录结构 |
| **Subagents** | 子 agent | 任务分工、能力隔离 | 专业任务、自动化流程 |
| **Skills** | 触发指令 | 单次流程 | 标准化操作流程 |

### 详细对比

#### Output Styles vs CLAUDE.md

| 特性 | Output Styles | CLAUDE.md |
|------|---------------|-----------|
| 作用范围 | 整个对话 | 项目级 |
| 主要内容 | 角色定义、行为规范 | 代码规范、目录结构 |
| 影响对象 | Claude 的整体行为 | 项目特定的约束 |
| 切换方式 | 通过命令切换 | 自动加载 |

**示例**：
- **Output Styles**："你是产品经理" → 改变 Claude 的角色
- **CLAUDE.md**："项目使用 2 空格缩进" → 定义项目规范

#### Output Styles vs Subagents

| 特性 | Output Styles | Subagents |
|------|---------------|-----------|
| 作用范围 | 主对话 | 子对话 |
| 上下文 | 共享 | 独立 |
| 调用方式 | 命令切换 | Task 工具 |
| 适用场景 | 改变整体角色 | 专业任务分工 |

**示例**：
- **Output Styles**：切换到"产品经理"角色
- **Subagents**：调用"developer" agent 实现功能

#### Output Styles vs Skills

| 特性 | Output Styles | Skills |
|------|---------------|--------|
| 作用范围 | 整个对话 | 单次流程 |
| 触发方式 | 命令切换 | 手动调用 |
| 持续性 | 持续生效 | 一次性 |
| 适用场景 | 改变角色 | 标准化流程 |

**示例**：
- **Output Styles**：持续以"讲解型"风格回复
- **Skills**：调用"代码审查"流程

## 🛠️ 实战场景

### 场景1：学习新代码库

**目标**：理解一个不熟悉的代码库

**步骤**：
1. 切换到 Explanatory 风格
   ```
   /output-style explanatory
   ```
2. 让 Claude 解释代码
   ```
   "解释一下 game.js 中的游戏循环是如何工作的？"
   ```
3. Claude 会提供详细的解释和 Insights

### 场景2：结对编程学习

**目标**：学习如何实现某个功能

**步骤**：
1. 切换到 Learning 风格
   ```
   /output-style learning
   ```
2. 提出需求
   ```
   "我想添加一个道具系统"
   ```
3. Claude 会插入 `TODO(human)` 让你参与

### 场景3：快速开发

**目标**：快速实现功能

**步骤**：
1. 使用 Default 风格（默认）
2. 直接提出需求
3. Claude 高效执行

### 场景4：产品规划

**目标**：规划产品功能

**步骤**：
1. 创建自定义 Output Style（产品经理）
2. 切换到该风格
3. 让 Claude 提供产品建议

## 🎓 最佳实践

1. **明确角色定位**：自定义 Output Style 时，明确 Claude 的角色
2. **保持一致性**：在项目中使用一致的 Output Style
3. **合理使用 keep-coding-instructions**：
   - 工程师角色设为 `true`
   - 非工程师角色设为 `false`
4. **提供清晰的规范**：在文件中明确说明行为规则
5. **测试效果**：创建后立即测试，确保符合预期
6. **版本控制**：将 `.claude/output-styles/` 加入版本控制

## 🔍 调试 Output Styles

### 检查当前风格

使用命令查看当前 Output Style：
```
/output-style
```

### 常见问题排查

#### 问题1：自定义风格未生效

**症状**：
- 切换后行为没有变化
- 仍然是默认风格

**解决方法**：
1. 检查文件路径是否正确
2. 确认 YAML 格式正确
3. 重启 Claude Code
4. 检查文件名和扩展名

#### 问题2：keep-coding-instructions 不起作用

**症状**：
- 设置为 `true` 但仍然不运行测试

**解决方法**：
1. 确认字段名正确（注意连字符）
2. 检查 YAML 格式
3. 查看是否有冲突的配置

#### 问题3：角色定义不清晰

**症状**：
- Claude 行为不符合预期
- 回复风格混乱

**解决方法**：
1. 在文件中明确定义角色
2. 提供具体的示例
3. 明确回复规范

## 🚫 常见错误

### 错误1：YAML 格式错误

❌ 错误：
```markdown
name: Game Developer
description: 贪吃蛇游戏开发专家
keep-coding-instructions: true
```

✅ 正确（必须有 `---` 分隔符）：
```markdown
---
name: Game Developer
description: 贪吃蛇游戏开发专家
keep-coding-instructions: true
---
```

### 错误2：字段名错误

❌ 错误：
```yaml
keepCodingInstructions: true  # 错误：应该是连字符
```

✅ 正确：
```yaml
keep-coding-instructions: true
```

### 错误3：缺少必要字段

❌ 错误：
```yaml
---
name: Game Developer
# 缺少 description
keep-coding-instructions: true
---
```

✅ 正确：
```yaml
---
name: Game Developer
description: 贪吃蛇游戏开发专家
keep-coding-instructions: true
---
```

## 📚 实战：为贪吃蛇创建 Output Style

### 创建文件

路径：`.claude/output-styles/snake-game-dev.md`

### 文件内容

```markdown
---
name: Snake Game Dev
description: 贪吃蛇游戏开发专家
keep-coding-instructions: true
---

# 游戏开发助手

你是贪吃蛇游戏的开发专家。

## 项目背景
- 纯前端游戏，使用 Canvas API
- 已实现：基础游戏、难度选择、双倍分数食物
- 不使用框架和第三方库

## 代码风格
- 2 空格缩进
- 驼峰命名
- 函数名以动词开头

## 工作流程
1. 修改代码前先 Read 文件
2. 修改后说明变更内容
3. 重要功能完成后更新 learning.md

## 回复规范
- 用中文回复
- 不添加 emoji
- 简洁明确
```

### 使用方式

1. 重启 Claude Code
2. 切换风格：
   ```
   /output-style snake-game-dev
   ```
3. 开始使用

## 🎓 七门课串联：完整工作流

### 概念关系图

```
┌─────────────────────────────────────────────────────────────────┐
│                        Claude Code 体系                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────┐    ┌─────────────────┐                    │
│  │ Output Styles   │    │   CLAUDE.md     │                    │
│  │ (我是谁？)      │    │  (项目规则)     │                    │
│  │ - 产品经理      │    │  - 代码风格     │                    │
│  │ - 工程师        │    │  - 目录结构     │                    │
│  │ - 运维专家      │    │  - 约束条件     │                    │
│  └────────┬────────┘    └────────┬────────┘                    │
│           │                      │                              │
│           └──────────┬───────────┘                              │
│                      ▼                                          │
│              ┌───────────────┐                                  │
│              │   主对话      │                                  │
│              │   (You)       │                                  │
│              └───────┬───────┘                                  │
│                      │                                          │
│           ┌──────────┼──────────┐                              │
│           ▼          ▼          ▼                              │
│    ┌──────────┐ ┌─────────┐ ┌─────────┐                        │
│    │  Skills  │ │  Hooks  │ │   MCP   │                        │
│    │(流程模板)│ │(自动触发)│ │(外部连接)│                        │
│    └─────┬────┘ └────┬────┘ └────┬────┘                        │
│          │           │           │                              │
│          ▼           │           │                              │
│    ┌─────────┐       │           │                              │
│    │SubAgent │       │           │                              │
│    │(专业分工)│       │           │                              │
│    └─────────┘       │           │                              │
│                      │           │                              │
│                      ▼           ▼                              │
│                 ┌────────────────┐                             │
│                 │   完成任务     │                             │
│                 └────────────────┘                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 一句话总结

| 概念 | 一句话描述 |
|------|-----------|
| **Output Styles** | 赋予你的角色（我是谁？） |
| **CLAUDE.md** | 定制规则手册（项目有什么规则？） |
| **Skills** | 调用后判断是否用 SubAgent 按步骤完成任务 |
| **Hooks** | 固定行为前后自动触发 |
| **MCP** | 按需自动调用，连接外部系统 |
| **SubAgents** | 独立上下文的专业代理 |
| **基本工具** | Read、Edit、Write、Grep、Glob |

### 完整工作流示例

```
场景：你想测试游戏功能

┌─────────────────────────────────────────────────────────────┐
│ 1. Output Styles 生效                                        │
│    "你是游戏开发专家"                                         │
└─────────────────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────────────────┐
│ 2. CLAUDE.md 加载                                            │
│    遵循项目规范：2 空格缩进、驼峰命名、不使用第三方库        │
└─────────────────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────────────────┐
│ 3. Hooks 自动触发                                            │
│    你发消息 → 自动显示 Git 状态                              │
└─────────────────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────────────────┐
│ 4. 你说："用 game-tester 测试游戏"                           │
└─────────────────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────────────────┐
│ 5. Skills 执行                                               │
│    读取 game-tester/SKILL.md                                 │
│    评估任务复杂度 → 判断需要浏览器测试                       │
└─────────────────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────────────────┐
│ 6. 调用 SubAgent                                             │
│    Task(subagent_type="tester", prompt="测试游戏")          │
└─────────────────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────────────────┐
│ 7. SubAgent 调用 MCP                                         │
│    需要 Chrome DevTools → 调用 chrome-devtools MCP           │
└─────────────────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────────────────┐
│ 8. 返回结果                                                  │
│    SubAgent → Skills → 你（收到测试报告）                   │
└─────────────────────────────────────────────────────────────┘
```

### 决策树

```
收到任务
    │
    ├─> 需要外部资源？
    │       ├─ 是 → 调用 MCP
    │       └─ 否 → 继续
    │
    ├─> 是标准化流程？
    │       ├─ 是 → 调用 Skills
    │       │      │
    │       │      └─> 复杂？
    │       │             ├─ 是 → 调用 SubAgent
    │       │             └─ 否 → 自己完成
    │       └─ 否 → 继续
    │
    └─> 自己完成
```

## 📚 下一步

尝试创建你自己的 Output Styles：
- 产品经理角色
- 运维专家角色
- 技术写作角色
- 代码审查专家角色

## 🔗 有用资源

- Claude Code GitHub: https://github.com/anthropics/claude-code
- 报告问题: https://github.com/anthropics/claude-code/issues

---

**更新日期**：2026-01-23